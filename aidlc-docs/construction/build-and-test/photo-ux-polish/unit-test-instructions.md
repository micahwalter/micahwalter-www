# Unit Test Execution — photo-ux-polish

## Status
**N/A** — this repository has no unit test runner / `test` script. Property-Based Testing extension is disabled for this engagement.

## Manual substitute
Validate pure helpers informally if needed:
- `buildOsmEmbedUrl` / `buildOsmBrowseUrl` in `lib/photos-api.ts`
- Tag match filtering in `components/PhotosGrid.tsx` (case-insensitive)

No automated unit suite to execute.
